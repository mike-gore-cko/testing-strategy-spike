version: "3.3"

services:    
  api:
    build:
      context: ../../
      dockerfile: src/Api/Dockerfile
    ports: 
      - 5000:5000

  localstack:
    image: localstack/localstack:1.0.4
    ports:
      - 4566:4566